---
interface Props {
  title: string
  image: string
}

const { title, image } = Astro.props
---

<div class="flex flex-row items-center w-full">
  <img src={`/images/${image}.png`} alt={image} class="size-8 md:size-12" />
  <h2 class="text-xl md:text-2xl font-bold">{title}</h2>
</div>
